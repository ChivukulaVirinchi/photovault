# Smriti Conference Audit TODO

Status key:
- [x] Audited and any confirmed issue fixed
- [ ] Pending audit or pending decision
- [~] Audited; no edit made, but keep an eye on it

## Explicit User Concerns

- [x] Visible viewport thumbnails must be fast-tracked.
  - Fixed urgent thumbnail queue overflow lane.
  - Fixed foreground photo thumbnail requests to avoid background priority.
  - Reduced prefetch thumbnail batch size so a visible cell cannot wait behind a long prefetch batch.
  - Fixed burst overview missing-cover thumbnails.
  - Fixed map marker thumbnails created outside Svelte.
- [~] Thumbnail quality must not degrade.
  - Current fixes keep Medium generation paths and existing quality settings.
  - Album/memory cover eager Medium upgrade is intentionally preserved for quality, though it can delay list response.
- [~] Older build opening newer DB must not be a dead end.
  - Settings route is accessible without an open library.
  - Welcome shows schema-too-new and links Settings.
  - Schema-too-new libraries now enter a separate unsupported-library state instead of returning a hard open error.
  - Added a read-only schema-version preflight before the normal writable database opener, so too-new libraries can reach the compatibility shell without first applying WAL/pragmas through normal open.
  - The UI keeps the selected drive visible and leaves Settings/update reachable without entering the normal app shell.
  - Added an allow-listed read-only photo preview that reads only stable `photos` columns over a read-only SQLite connection and uses existing thumbnail paths only.
  - Compatibility decision: do not reuse the normal `OpenLibrary` state for this. It contains a writable `Database` plus thumbnail/semantic services and would let write commands/background jobs run against an unsupported schema.
  - Full normal-shell browsing on unknown future schemas is classified as a future compatibility expansion, not a conference blocker, because it requires command-by-command schema compatibility proof.
- [x] Precommit/security issue from `cargo audit`.
  - `quinn-proto` vulnerability addressed earlier.
  - `cargo audit` rerun exits 0.
  - 19 unmaintained/unsound warnings remain allowed warnings unless policy changes.

## Frontend Routes

- [x] `Timeline.svelte`
  - Audited thumbnail visible path, paging, browse context, selection/trash undo, and large-scroll behavior.
  - Fixed stale/deleted deep-scroll offsets so offset-addressed loads retry the last valid timeline page instead of rendering a blank window.
  - Added photo-anchor return restoration from PhotoDetail and a dismissible full-library thumbnail prompt.
  - Fixed large-library scrubber visibility to use estimated full timeline height rather than only the loaded virtual rows.
  - Fixed failed return-anchor restores to clear stale anchors so Timeline falls back to normal loading on later visits.
  - Tightened PhotoDetail back-navigation restore to keep the clicked photo at its prior viewport offset instead of recentering after thumbnail/layout shifts.
  - Fixed marquee-selection RAF/auto-scroll cleanup when leaving Timeline mid-drag.
  - Fixed pointer-capture release when Timeline marquee selection is cancelled or the route is torn down mid-drag.
  - Fixed scroll listener/resize cleanup to detach from the exact element that was observed.
  - Fixed scan/metadata/thumbnail job starts so route navigation after click cannot drop the real job chip when IPC returns.
  - Fixed generic route-return restoration so offset-window hydration preserves the exact saved scroll position instead of snapping to a page boundary.
- [x] `PhotoDetail.svelte`
  - Audited detail loading, slideshow/nav context, member thumbnails, video probe, and stale sequence guards.
  - Fixed preloading so URL resolution alone does not suppress image decode/readiness work; added an in-flight guard for rapid navigation.
  - Kept eagerly mounted from `App.svelte` after regression triage; photo open reliability is more important than route chunking here.
  - Follow-up: resolved original-file URLs now survive PhotoDetail remounts, reducing cold reopen cost from grids.
- [x] `Albums.svelte`
  - Audited suggestions, cover thumbnail requests, preview modal, job completion handling.
  - Fixed album-suggestion start placeholders so library switches always clear the pending chip before suppressing stale job registration.
  - Follow-up: album-list scroll restore now runs after album/suggestion rows are loaded.
- [x] `AlbumDetail.svelte`
  - Audited paged loading, all-id cap for navigation, selection, remove/trash undo, export events, slideshow.
  - Fixed scroll listener cleanup to detach from the exact element that received the listener.
  - Follow-up: saved scroll now restores after enough paged photo rows are loaded, so closing PhotoDetail returns to the same album position.
- [x] `Duplicates.svelte`
  - Fixed duplicate list/wasted-space load parallelism.
  - Fixed stale `loadingMore` state when job/progress reload races with a paged load.
  - Fixed duplicate-scan job registration so same-library navigation does not leave a pending placeholder, while library switches suppress stale job chips.
  - Fixed duplicate-scan start placeholders so library switches always clear the pending chip before suppressing stale job registration.
  - Follow-up: tab scroll is persisted and restored after enough groups are reloaded.
  - Thumbnail visible path already present.
- [x] `DuplicateDetail.svelte`
  - Audited stale mutation guards, thumbnail visible path, trash/dismiss flows.
- [x] `Bursts.svelte`
  - Fixed missing-cover thumbnail generation for visible burst cards.
  - Fixed stale `loadingMore` state when job/progress reload races with a paged load.
  - Fixed burst-detection job registration so same-library navigation does not leave a pending placeholder, while library switches suppress stale job chips.
  - Fixed burst-detection start placeholders so library switches always clear the pending chip before suppressing stale job registration.
  - Follow-up: tab scroll is persisted and restored after enough groups are reloaded.
  - Audited pagination/job completion.
- [x] `BurstDetail.svelte`
  - Audited stale mutation guards and thumbnail visible path.
- [x] `People.svelte`
  - Audited face processing job state, live updates, pending/review counts, model-upgrade flow.
  - Fixed face-detection job registration so immediate navigation after clicking Find faces does not hide the real job until the first backend progress event.
  - Follow-up: People tab scroll position is persisted across route changes.
- [x] `PersonDetail.svelte`
  - Audited paged photos, verify strip, reassign/merge/similar dialogs, selection/trash.
  - Removed a stale face-count subtitle branch that referenced a field not present in the Rust `PersonDto`.
  - Follow-up: saved scroll now restores after enough paged person-photo rows are loaded.
- [x] `FaceReview.svelte`
  - Audited batch review flow and skipped-face handling.
- [x] `PersonReview.svelte`
  - Audited queue review refill and keyboard actions.
- [x] `Search.svelte`
  - Audited bounded rendering, selection/trash undo, thumbnail path for photo/album hits.
  - Added a clear-search icon that also clears Search route/query state.
- [x] `Map.svelte`
  - Fixed missing thumbnail requests for MapLibre marker DOM.
  - Audited all-pins fallback to viewport mode and drawer thumbnail requests.
  - Fixed stale map-marker thumbnail requests so removed pins do not keep queued work during pan/zoom.
  - Fixed direct single-pin photo opens to reset PhotoDetail navigation to a map-scoped context instead of inheriting stale timeline/album prev-next state.
  - Fixed Map route teardown to remove its global Escape-key listener when leaving the page.
  - Fixed all-pins fallback so it cannot clear errors after the Map route has been destroyed.
  - Kept eagerly mounted from `App.svelte` after regression triage to remove async route-load failure/latency from the map path.
- [x] `Memories.svelte`
  - Audited disabled-setting guard and hero thumbnail visible path.
  - Fixed internal `fallback_window` memory kind text to display as a user-facing label.
  - Follow-up: Memories tab scroll position is cached across route changes.
- [x] `MemoryDetail.svelte`
  - Audited photo grid thumbnail path and memory album save flow.
  - Fixed scroll listener cleanup to detach from the exact element that received the listener.
  - Follow-up regression triage fixed stale shared marquee click suppression that could swallow later photo opens after a selection drag.
  - Follow-up: saved scroll now restores after the memory grid is populated, so closing PhotoDetail returns to the same memory position.
- [x] `Insights.svelte`
  - Audited backend offload and representative image paths.
- [x] `Trash.svelte`
  - Audited cursor paging, restore/delete behavior, thumbnail visible path.
- [x] `Settings.svelte`
  - Audited no-library access, update controls, rebuild thumbnail action, asset/setup actions.
  - Fixed no-library visual-search asset status to use global asset inventory instead of showing false missing state when semantic DB status is unavailable.
  - Fixed no-library semantic model readiness to require both visual and text model assets, not a partial install.
  - Fixed no-library visual-search recheck action to refresh global asset inventory instead of no-oping through library-scoped semantic status.
  - Fixed library-scoped Settings actions so delayed IPC returns cannot re-register stale job chips or apply exclusion prompts after the user switches/closes libraries.
  - Extended same-library guards to exclusion removal, stack refresh, face resets, cluster reset, and suggestion reset so delayed returns cannot toast/update the wrong library.
  - Fixed face reset/recluster follow-up job registration to dismiss stale placeholders and register real jobs for the same active library even if Settings is no longer visible.
  - Fixed geocoding, metadata refresh, thumbnail regeneration, and visual-search indexing starters so stale-library returns clear placeholders before suppressing real job registration.
- [x] `Welcome.svelte`
  - Audited schema-too-new handling and Settings access without library.
  - Fixed read-only compatibility preview so zero-photo libraries do not trigger an endless reload loop.
  - Fixed read-only compatibility preview so failed preview loads stop retrying and do not leave a stale Load more button.
- [x] `Shortcuts.svelte`
  - Quick audit only; no dynamic risk found.
- [x] Removed/deferred routes
  - `Cull.svelte`, `Documents.svelte`, `Health.svelte` are currently deleted/deferred in this worktree.

## Shared Frontend Libraries / Components

- [x] `thumbnailQueue.ts`
  - Fixed visible-request urgent overflow.
  - Increased urgent viewport bypass capacity so a large scroll jump is not stuck behind stale prefetch batches.
  - Added/ran queue tests.
- [x] `thumbnailRequest.ts`
  - Audited two-observer visible/prefetch behavior and batching.
  - Reduced batch size from 6 to 2 to lower worst-case visible wait when a cell was already in a prefetch batch.
- [x] `thumbnail.ts`
  - Fixed thumbnail URL resolution to reject absolute, traversal, and malformed DB-sourced thumbnail paths before `convertFileSrc`.
- [x] `virtualizer.svelte.ts`
  - Fixed avoidable large-library scroll work by separating row-offset construction from scroll-window calculation.
- [x] `videoProbe.ts`
  - Audited video poster save path.
- [x] `stores/library.svelte.ts`
  - Audited schema-too-new preservation and remembered drive persistence.
- [x] `stores/jobs.svelte.ts`
  - Audited placeholder dismissal and global job state.
  - Fixed stale library-scoped job chips when switching or closing libraries.
  - Fixed semantic job scoping so library switches clear visual-search indexing without hiding global model downloads.
  - Fixed late progress events from cancelled old-library jobs so they cannot recreate cleared job chips after a library switch.
- [x] `JobsIndicator.svelte`
  - Fixed global cancel affordance for album-suggestion detection; the backend already honors the job cancel flag.
- [x] `stores/selection.svelte.ts`
  - Audited scoped selection behavior.
- [x] `stores/photoVisibility.svelte.ts`
  - Audited trash/restore invalidation.
- [x] `stores/slideshow.svelte.ts`
  - Audited paged load-more slideshow flow.
  - Fixed stale `loadingMore` state after closing/restarting while a paged load is still in flight.
- [x] `Slideshow.svelte`
  - Fixed overlapping neighbor preloads to avoid repeated decode/readiness work during normal next/previous navigation.
  - Fixed current slide loads to reuse completed preload readiness instead of decoding the same image again.
  - Fixed stale neighbor preload completions so closed/restarted slideshows cannot repopulate readiness state from an old session.
- [x] `AddToAlbumDialog.svelte`
  - Audited bulk add bounds via backend.
- [x] `MergePersonDialog.svelte`
  - Audited cannot-merge handling and target search.
- [x] `ReassignFaceDialog.svelte`
  - Audited suggestions and all-people search.
- [x] `KSimilarDialog.svelte`
  - Audited confirm/reject flow.
- [x] `JobsIndicator.svelte`
  - Audited cancel IPC shape.
- [x] `AssistantDrawer.svelte`
  - Audited heavy assistant operations moved off shared DB earlier.
- [x] Remaining low-risk UI utilities/components
  - `ToastHost.svelte`, `Sidebar.svelte`, `dominantColor.ts`, and small CSS-only surfaces got a final quick pass; no concrete runtime issue found.
  - `ZoomImage.svelte` got a follow-up pass after PhotoDetail preload changes.
  - Follow-up found and fixed a `ZoomImage.svelte` WebView decode-hang risk: the viewer no longer waits indefinitely on `img.decode()` before swapping images.
  - Added cleanup for abandoned `ZoomImage.svelte` off-screen image timers/handlers during rapid next/previous navigation.
  - Fixed shared marquee selection pointer-capture release when a grid route is destroyed mid-drag.
  - Follow-up regression triage narrowed marquee click suppression to the immediate post-drag click only, preventing later normal clicks from being blocked.
  - Follow-up regression triage shortened `ZoomImage.svelte` blank-screen fallback and allows a forced visible swap when WebView image metadata is slow.
  - Follow-up: `ZoomImage.svelte` now uses DB-provided image dimensions when WebView metadata is slow, preventing forced 1x1 fallback from opening photos hugely zoomed with no way to zoom out.

## Active Regression Triage

- [x] Memories photo clicks crash/fail to open after marquee selection.
  - Removed async photo route loading from `App.svelte`.
  - Fixed stale marquee click suppression.
  - Follow-up: memory photo anchors now opt out of marquee pointer capture and refresh memory browse context before normal open.
- [x] Blank black photo viewer / very slow first visible image.
  - Fixed `ZoomImage.svelte` forced fallback so a slow metadata/decode path does not leave the viewer blank.
  - Follow-up: restored PhotoDetail thumbnail first paint behind the full image and stopped waiting for full file URL resolution before painting the photo shell.
- [x] Timeline return lands correctly then jumps away.
  - Restored scroll positions now update Timeline's `scrollTop` state/cache immediately, so hydration does not run from stale coordinates.
  - Follow-up: the generic saved-scroll restore is now disabled from initial component setup when a photo-return anchor exists, preventing an early race before `onMount`.
  - Follow-up: programmatic Timeline restores now update the virtualizer's internal scroll state synchronously instead of waiting for the next scroll RAF.
  - Follow-up: generic Timeline restore now falls back to the in-memory Timeline cache when session scroll storage is missing, covering route/tab switches back to Timeline.
  - Follow-up: Timeline now snapshots scroll state during route teardown and eager-loads the currently rendered virtualized thumbnails to reduce the post-return blank thumbnail gap.
  - Follow-up: generic Timeline hydration now reloads around the saved absolute scroll position without overwriting that position, covering tab/route switches back to Timeline.
  - Follow-up: Timeline scroll persistence now keeps a module-level last-known offset and guards teardown/remount paths from overwriting it with an incidental `0`.
  - Follow-up: Timeline keeps visible thumbnail images warm in memory during route changes to reduce blank-grid repaint on return.
- [x] Photo opening from non-Timeline routes.
  - Album, Search, and Person detail grids now refresh their browse context on normal photo opens, matching Timeline/Memory behavior.
  - Favourites smart album now routes through `#/album?id=-1` correctly by allowing signed album ids only for album routes.
  - Search route now caches query/results/visible count/scroll so PhotoDetail back returns to the same search state even when the query was typed locally and not encoded in the URL.
  - Albums route now caches suggestion preview state so opening a suggestion photo and backing out returns to the preview dialog.
  - Map route now caches the active cluster drawer/filmstrip across PhotoDetail round-trips.
  - Insights route now caches computed data, selected year, expansion toggles, and scroll across PhotoDetail round-trips.
- [x] Marquee selection regression.
  - Restored the known-good shared marquee implementation from the prior commit shape: anchors are excluded from marquee starts, native blue text selection is prevented during marquee, and selection replacement remains reactive.
  - Deselecting the final selected photo now blurs the cell and Timeline clears its focused-cell artifact when selection becomes inactive.
- [x] Slideshow video behavior.
  - Video slides no longer use the photo interval timer.
  - Visible videos advance on native `ended`; inactive buffered videos are paused/reset so audio cannot continue under the next slide.
- [x] Windows reveal-in-folder fallback.
  - If Shell selection fails for a valid path, Smriti now opens the containing folder instead of surfacing the raw `ILCreateFromPathW returned NULL` error.
  - Follow-up: Windows reveal now normalizes extended-length paths and tries Explorer `/select` before falling back to the parent folder.
- [x] People tab false empty state.
  - People now shows a loading state while `people.list()` is in flight instead of briefly showing "No faces yet."
- [x] Map route crash suspected from async route load path.
  - Removed async map route loading from `App.svelte`.
- [ ] Needs manual runtime smoke test on a large real library: Memories open/selection, Timeline open-back stability, Map open, People/Insights consistency, large image next/previous latency.

## Tauri Commands / Backend Surfaces

- [x] Semantic ONNX warm-up
  - Existing library-open warm-up is now also triggerable from the frontend after an already-open library is discovered.
  - Warm-up now uses the same runner-before-index lock order as search to avoid startup contention.

- [x] `photos.rs`
  - Fixed timeline/list paging off shared DB.
  - Fixed album/person/date/place paged helper off shared DB.
  - Fixed batch thumbnail generation to background priority.
  - Fixed offset-addressed timeline pages to compute `has_more` from total count instead of cursor-page fullness.
  - Fixed cursor-paged photo lists to fetch one extra row before setting `has_more`, avoiding an empty follow-up request on exact-size final pages.
- [x] `albums.rs`
  - Audited bulk bounds, smart album rejection, export, suggestions jobs.
  - Preserve eager Medium cover upgrade for quality; performance tradeoff noted.
  - Fixed album-suggestion detection job cleanup to use a captured Tokio runtime handle, so blocking-thread exits cannot leave the job registered forever.
  - Fixed album export source resolution to refuse symlink escapes outside the library root before copying files.
  - Dedicated command pass rerun; no additional concrete edit found.
- [x] `duplicates.rs`
  - Fixed list and wasted-space reads to secondary DB.
  - Audited stale resolved-group mutations.
  - Dedicated command pass checked live-persist cancellation semantics; duplicates intentionally preserve already-found groups on cancel.
  - Fixed duplicate-detection job cleanup to use a captured Tokio runtime handle, so blocking-thread exits cannot leave the job registered forever.
- [x] `bursts.rs`
  - Fixed list reads to secondary DB.
  - Audited stale resolved-group mutations.
  - Fixed cancelled burst detection to remove only live groups inserted by that cancelled run, preventing partial results from sticking around.
  - Fixed burst-detection job cleanup to use a captured Tokio runtime handle, so blocking-thread exits cannot leave the job registered forever.
- [x] `people.rs`
  - Fixed heavy face-suggestion/similar-face reads to secondary DB.
  - Audited face job completion, reset, stats write library guard.
  - Fixed paged face list/review endpoints to fetch one extra row for `has_more` and stop reporting page length as a fake total.
- [x] `search.rs`
  - Audited main query off shared DB and result bounds.
- [x] `map.rs`
  - Audited all-pins cap, viewport fallback, antimeridian bounds, filmstrip bounds, tile-cache commands.
- [x] `memories.rs`
  - Fixed disabled-setting guard earlier.
  - Fixed save-as-album to reject blank explicit names and trim accepted names, matching normal album creation.
- [x] `insights.rs`
  - Audited off-thread compute, secondary DB use, hero thumbnail resolution, and face crop paths.
- [x] `geocoding.rs`
  - Audited background backfill, GeoNames setup, cancellation behavior, and lat/lng validation.
- [x] `semantic.rs`
  - Audited indexing/query background behavior and bounds.
- [x] `assistant.rs`
  - Audited off-thread heavy search, bounds, library-root guard.
- [x] `documents.rs`
  - Audited secondary DB list/search, cursor paging, FTS literal query handling, and category mutation.
  - Fixed deferred document list/search pages to fetch one extra row for `has_more`, matching the active paged command behavior.
- [x] `trash.rs`
  - Fixed permanent-delete statement lifetime earlier.
  - Audited cursor paging and restore/delete operations.
  - Fixed permanent delete to refuse DB paths that resolve outside the library root through symlinked parents.
- [x] `stacks.rs`
  - Audited stale dismissed-stack mutations.
- [x] `settings.rs`
  - Audited no-library access.
- [x] `system.rs`
  - Audited update check and asset health no-library access.
  - Fixed reveal/copy path resolution to require the existing file to canonicalize under the library root.
- [x] `library.rs`
  - Audited library root validation, schema-too-new mapping, background-library guards.
  - Fixed newer-schema detection to run as a read-only preflight before normal writable database open.
  - Fixed read-only compatibility preview pages to compute `has_more` from offset and total count.
  - Fixed post-scan metadata/thumbnail stages to skip if the user switched libraries before they start.
  - Fixed thumbnail-regeneration and photo-date-refresh commands to reserve their job slot before resetting photo rows, avoiding conflict-after-mutation races.
- [x] `state.rs`
  - Fixed library close/open job cleanup so cancelled old-library jobs are detached from the backend registry and cannot block jobs in the next library.
- [x] Final command registration/API parity check
  - Rerun found no frontend command strings missing Tauri handlers.
  - Some registered commands are currently unused by the active UI; they are deferred/admin surfaces, not broken invokes.
  - Latest rerun again found no missing frontend-invoked Tauri commands.
  - Latest robust parser pass found 141 frontend-invoked commands, 153 registered handlers, and 0 missing handlers.
  - Static hash-route extraction found no links to missing `App.svelte` route branches.
  - Updated `docs/COMMAND_SURFACE.md` for the newer-schema read-only compatibility shell and `library.compat_photos_list`.
  - Corrected `SchemaTooNewDto` docs to match the actual signed Rust DTO fields.

## Rust Engine / DB / Services

- [x] `db/duplicate_repo.rs`
  - Fixed stale dismissed/resolved mutation protections.
- [x] `db/burst_repo.rs`
  - Fixed stale dismissed/resolved mutation protections.
  - Added exact-member-set cleanup support for cancelled live burst persistence.
- [x] `db/stack_repo.rs`
  - Fixed dismissed-stack mutation protections.
- [x] `db/album_repo.rs`
  - Fixed album navigation order to match visible grid.
  - Audited bulk add/remove live rows and cover refresh.
- [x] `db/trash_repo.rs` / `services/trash.rs`
  - Audited cursor/restore/delete paths.
  - Added a symlink-escape regression for permanent delete so a poisoned DB path cannot remove a file outside the library root.
- [x] `db/face_repo/*`
  - Audited gallery/face read/write flows used by people UI.
- [x] `services/thumbnail.rs`
  - Audited foreground/background priority behavior, quality guard, and cache validation.
  - Fixed valid low-byte thumbnails from simple images being rejected and regenerated repeatedly.
  - Fixed duplicate thumbnail requests so deduper waiters do not consume scarce generation permits while another thread is already generating the same thumbnail.
- [x] `services/tile_cache.rs`
  - Fixed fetched map tiles not enforcing the configured disk-cache limit.
- [x] `services/face_processor.rs`
  - Audited current-library guard, thumbnail-assisted face processing, and cancellation tail behavior.
  - Fixed cancelled face runs to stop before contextual propagation/clustering after already-processed chunks are flushed.
- [x] `services/album_suggestions.rs`
  - Audited cancellation, diagnostics, city/country grouping, and malformed date handling.
  - Fixed trip suggestions merging same city names across different countries.
  - Fixed malformed short `date_taken` strings panicking trip/event/diagnostic parsing.
  - Fixed trip home-distance checks across the international date line.
- [x] `services/duplicate_detector.rs`
  - Audited background detection and wasted-space calculation.
  - Fixed broad-clippy cleanliness by factoring exact duplicate candidate rows into a private type alias and using `sort_by_key`.
- [x] `services/burst_detector.rs`
  - Audited streaming persistence, thumbnail-assisted signatures, and timestamp ordering.
  - Fixed burst grouping to sort by parsed UTC time rather than raw timestamp text, avoiding false groups across mixed offsets/formats.
- [x] `services/semantic.rs`
  - Audited bounded indexing/query behavior and active-library status counts.
  - Fixed semantic index stats to ignore trashed photos when reporting indexed/failed counts.
- [x] `services/geocoding.rs`
  - Audited background setup/backfill.
  - Fixed reverse-geocoding lookup near the international date line by splitting wrapped longitude ranges.
- [x] `services/map_math.rs`
  - Audited projection/tile math; current tests pass and active MapLibre route does not depend on it for rendering.
- [x] `services/insights.rs`
  - Audited top-location grouping fix.
  - Fixed dashboard city count to match city/country top-location grouping.
- [x] `services/reindexer.rs`
  - Audited wrong-library/background write guards.
  - Fixed move detection to normalize discovered candidate paths with DB storage separators on Windows.
- [x] `services/raw_preview.rs`, `image_io.rs`, `metadata_processor.rs`
  - Audited decode/metadata paths at high level.
  - Fixed RAF decode routing to return an explicit unsupported-container error instead of treating it as TIFF RAW.
  - Ran RAW preview corruption-guard tests and metadata chunk/video-date tests.
- [x] `db/migrations.rs`
  - Schema-too-new guard works and is tested.
  - Safe unsupported-library shell is implemented.
  - Minimal read-only preview is implemented.
  - Added a compatibility-safe timeline order index ensure step without bumping `schema_version`, so older builds are not blocked by a performance-only index.
  - Query-plan regression verifies the main timeline order uses `idx_photos_timeline_order` without a temp B-tree sort.
  - Added a compatibility-safe pending-face review index ensure step without bumping `schema_version`.
  - Query-plan regression verifies cluster-scoped pending-face review uses `idx_faces_review_pending` without a temp B-tree sort.
  - Added a compatibility-safe Favorites album order index ensure step without bumping `schema_version`.
  - Query-plan regression verifies Favorites paging uses `idx_photos_favorite_order` without a temp B-tree sort.
  - Added a compatibility-safe GPS bounds index ensure step without bumping `schema_version`.
  - Query-plan regression verifies map viewport bounds can use `idx_photos_gps_bounds`.
  - Follow-up audit verified fresh-schema indexes and migration ensure indexes match for the added hot paths.
  - Full read-only app browsing remains future work until each command is proven compatible with newer schema versions.
  - Safe design documented above; do not bypass `SchemaTooNewError` into the normal writable library state.
- [x] Final panic/TODO/unwrap scan
  - Rerun found no TODO/FIXME/HACK markers in main source trees.
  - Panic hits are test assertions.
  - Runtime unwrap/expect hits are invariant/startup cases; no targeted low-noise edit made.

## Verification Completed

- [x] `cargo fmt --check`
- [x] `cargo test -p smriti services::album_suggestions::tests`
- [x] `cargo test -p smriti services::burst_detector::tests`
- [x] `cargo check -p smriti`
- [x] `cargo test -p smriti services::semantic::tests`
- [x] `cargo test -p smriti services::thumbnail::tests`
- [x] `cargo test -p smriti db::burst_repo::tests`
- [x] `cargo check -p smriti-tauri`
- [x] `cargo test -p smriti-tauri commands::albums::tests --no-fail-fast`
- [x] `cargo test -p smriti db::duplicate_repo::tests`
- [x] `cargo test -p smriti db::document_repo::tests`
- [x] `cargo test -p smriti-tauri commands::geocoding::tests`
- [x] `cargo test -p smriti-tauri commands::memories::tests`
- [x] `cargo test -p smriti-tauri commands::map::tests`
- [x] `cargo test -p smriti services::insights::tests`
- [x] `npm run check --prefix src-ui`
- [x] Manual local CI gate equivalent after `scripts/ci_local.sh ci` hit CRLF/bash wrapper issue on Windows:
  - `cargo fmt --all --check`
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui`
  - `npm run test --prefix src-ui` (10 files, 79 tests at the time)
  - `npm run build --prefix src-ui`
  - `cargo clippy --all-targets -p smriti -p smriti-tauri -- -D warnings`
  - `cargo test -p smriti -p smriti-tauri`
- [x] Post-compat broad verification:
  - `cargo fmt --all --check`
  - `npm run build --prefix src-ui`
  - `cargo clippy --all-targets -p smriti -p smriti-tauri -- -D warnings`
  - `cargo test -p smriti -p smriti-tauri`
  - `cargo audit`
  - `git diff --check`
  - command registration parity scan
- [x] `cargo test -p smriti-tauri --lib photos`
  - Latest rerun: 4 tests passed.
- [x] `cargo test -p smriti-tauri --lib people`
- [x] `cargo test -p smriti-tauri --lib bursts`
- [x] `cargo test -p smriti-tauri --lib duplicates`
- [x] `cargo test -p smriti-tauri --lib commands --no-fail-fast`
- [x] `cargo test -p smriti-tauri --lib memories`
- [x] `cargo test -p smriti-tauri --lib library`
  - Latest rerun: 5 tests passed.
- [x] `cargo test -p smriti-tauri --test dto_snapshots`
- [x] `cargo clippy --all-targets -p smriti-tauri -- -D warnings`
- [x] `npm run test --prefix src-ui`
  - Latest rerun: 10 files, 81 tests.
- [x] Command registration parity for `library_compat_photos_list`
- [x] `cargo test -p smriti --lib db::duplicate_repo`
- [x] `cargo test -p smriti --lib db::burst_repo`
- [x] `cargo test -p smriti --lib db::stack_repo`
- [x] `cargo test -p smriti --lib services::trash`
- [x] `cargo test -p smriti services::duplicate_detector::tests` (5 tests passed)
- [x] `npm run test --prefix src-ui -- thumbnailQueue`
- [x] `npm run test -- --run src/lib/thumbnailQueue.test.ts` from `src-ui`
- [x] `npm run check` from `src-ui`
- [x] `git diff --check` on touched files after focused edits
- [x] Latest focused thumbnail path rerun:
  - `npm ci --prefix src-ui`
  - `npm run test --prefix src-ui -- --run src/lib/thumbnail.test.ts`
  - `npm run check --prefix src-ui`
- [x] Latest focused frontend lifecycle rerun:
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui`
- [x] Latest focused Tauri command rerun:
  - `cargo fmt --all --check`
  - Initial `cargo check -p smriti-tauri` correctly failed because `src-ui/dist` was absent for `tauri::generate_context!()`.
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - `cargo check -p smriti-tauri`
- [x] Latest focused Rust reruns:
  - `cargo fmt --all --check`
  - `cargo test -p smriti services::thumbnail::tests` (8 tests passed)
  - `cargo audit` (0 vulnerabilities; 19 allowed warnings)
- [x] Latest focused filesystem-safety rerun:
  - `cargo fmt --all --check`
  - `cargo test -p smriti --lib services::path_util::tests` (6 tests passed)
  - `cargo test -p smriti --lib services::trash::tests` (4 tests passed)
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - `cargo check -p smriti-tauri`
  - `git diff --check`
- [x] Latest focused job-state rerun:
  - `npm ci --prefix src-ui`
  - `npm run test --prefix src-ui -- --run src/lib/stores/jobs.test.ts` (6 tests passed)
  - `npm run check --prefix src-ui`
  - `git diff --check`
- [x] Latest focused timeline/viewer rerun:
  - Confirmed the full-library thumbnail prompt has a persisted dismiss X.
  - Confirmed PhotoDetail uses the existing viewer/slideshow preload paths rather than adding a separate broad mechanism.
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `npm run test --prefix src-ui -- --run src/lib/slideshowQueue.test.ts src/lib/thumbnailQueue.test.ts src/lib/decodeOffscreen.test.ts` (3 files, 27 tests)
- [x] Latest focused lifecycle parity rerun:
  - Command-surface parser found 153 documented commands, 153 registered handlers, 141 frontend-invoked commands, and 0 missing in any direction.
  - Normalized source inventory found 183 source files and 0 missing from `AUDIT_TODO.md`.
  - Fixed a leaked `Map.svelte` global keydown listener.
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `git diff --check -- src-ui/src/routes/Map.svelte AUDIT_TODO.md`
- [x] Latest focused stale-async guard rerun:
  - Exact frontend event-listener cleanup scan found no unmatched `addEventListener` calls.
  - Timer/RAF/observer cleanup scans found no missing cleanup variables.
  - Fixed stale Map fallback update and Settings library-scoped delayed-return guards.
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `git diff --check -- src-ui/src/routes/Map.svelte src-ui/src/routes/Settings.svelte AUDIT_TODO.md`
  - Event-listener cleanup script reported `event_listener_missing_removes=0`.
- [x] Latest focused backend-event/listener rerun:
  - Normalized scan found 24 backend-emitted event topics and 0 emitted topics without a frontend listener.
  - Confirmed `thumbnail:ready` is actively listened to by Timeline and job progress events flow through the global jobs store.
  - Removed stale docs/constants for non-emitted update, drive-change, scan-recommended, and documents job events.
  - Removed the dead frontend `documents:*` job subscriptions.
  - Rerun found 24 backend-emitted event topics, 24 frontend-listened topics, 0 emitted-unlistened topics, and 0 listened-unemitted topics.
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
- [x] Latest focused command-surface parity after event cleanup:
  - Canonical parser found 153 registered Tauri handlers, 153 documented commands, 130 frontend-invoked commands, and 0 missing in any direction.
  - Stale event/command scan found no `system.updates.download`, `system.updates.install`, `documents.run_analysis`, `drives:changed`, `update:*`, `library:scan-recommended`, or `documents:*` job-event references outside active document command names.
  - Production-side panic/debug scan found no `test.only`, `describe.only`, `debugger`, `console.log`, `todo!`, `unimplemented!`, or data-dependent `panic!` sites; remaining `unwrap`/`expect` hits are tests, static regex construction, compile-time constants, or valid date/time construction.
  - `npm run test --prefix src-ui -- --run src/lib/stores/jobs.test.ts` (6 tests passed)
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
- [x] Latest focused API type drift rerun:
  - Found and fixed stale frontend `PersonDto.face_count`; Rust sends `representative_face_id` instead.
  - Command-surface parser still found 153 documented commands, 153 registered handlers, 141 frontend-invoked commands, and 0 missing in any direction.
  - Route-link scan flagged only `/timeline`, which is handled by App's fallback Timeline branch.
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `git diff --check -- src-ui/src/lib/api/types.ts src-ui/src/routes/PersonDetail.svelte AUDIT_TODO.md`
- [x] Latest focused command-argument shape rerun:
  - TypeScript AST scan found 144 frontend `call`/`invoke` call sites and 0 non-snake-case command argument keys.
  - Duplicate exported frontend API type scan found 0 duplicate names under `src-ui/src/lib/api`.
  - Shared Rust/TypeScript DTO name scan covered 16 same-named DTO interfaces after the `PersonDto` fix.
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `git diff --check -- AUDIT_TODO.md src-ui/src/lib/api/types.ts src-ui/src/routes/PersonDetail.svelte`
- [x] Latest focused marquee lifecycle rerun:
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
- [x] Latest focused album-suggestions job cleanup rerun:
  - `cargo fmt --all --check`
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - `cargo check -p smriti-tauri`
- [x] Latest focused duplicates/bursts job cleanup rerun:
  - `cargo fmt --all --check`
  - Initial Tauri test filters correctly failed because `src-ui/dist` was absent for `tauri::generate_context!()`.
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - `cargo test -p smriti-tauri --lib duplicates` (compiled; 0 matching tests)
  - `cargo test -p smriti-tauri --lib bursts` (compiled; 0 matching tests)
- [x] Latest focused job-registry cleanup rerun:
  - `cargo fmt --all --check`
  - Initial Tauri test filter correctly failed because `src-ui/dist` was absent for `tauri::generate_context!()`.
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - `cargo test -p smriti-tauri --lib state` (1 test passed)
- [x] Latest focused job-cancel UI rerun:
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
- [x] Latest focused Settings library-scope rerun:
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `git diff --check -- src-ui/src/routes/Settings.svelte AUDIT_TODO.md`
- [x] Latest focused library-scoped job registration rerun:
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `git diff --check -- src-ui/src/routes/Settings.svelte src-ui/src/routes/Duplicates.svelte src-ui/src/routes/Bursts.svelte AUDIT_TODO.md`
- [x] Latest focused newer-schema preflight rerun:
  - `cargo fmt --all --check`
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - Initial focused test compile caught a test-only use of a non-reexported helper; fixed to use public `db_path_for`.
  - `cargo test -p smriti-tauri --lib preflight_schema_too_new_reads_existing_db_without_normal_open` (1 test passed)
  - `rg` check confirmed `SchemaTooNewDto` docs match signed Rust DTO fields.
  - `git diff --check -- src-tauri/src/commands/library.rs docs/COMMAND_SURFACE.md AUDIT_TODO.md`
- [x] Latest focused pagination rerun:
  - Removed exact-limit `has_more` handling from command pages that lacked a total count or `limit + 1` probe.
  - `cargo fmt --all --check`
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `cargo test -p smriti-tauri --lib photos` (4 tests passed)
  - `cargo test -p smriti-tauri --lib people` (1 test passed)
  - `cargo test -p smriti-tauri --lib documents` (compiled; 0 matching tests)
  - `scripts\clean_builds.ps1` reported `Before: 1.5 GB`, `After: 1.5 GB`.
- [x] Latest focused timeline-index rerun:
  - Added `idx_photos_timeline_order` for `WHERE is_trashed = 0 ORDER BY date_taken IS NULL, date_taken DESC, id DESC`.
  - Added `idx_faces_review_pending` for pending face-review queries scoped by `user_confirmed` and `cluster_id`.
  - Added `idx_photos_favorite_order` for the virtual Favorites album paging order.
  - Added `idx_photos_gps_bounds` for map viewport filtering over geotagged photos.
  - Existing libraries get these indexes from `run_migrations` without a schema-version bump.
  - `cargo fmt --all --check`
  - `cargo test -p smriti --lib db::schema::tests::test_create_schema` (1 test passed)
  - `cargo test -p smriti --lib db::migrations::tests::run_migrations_adds_timeline_order_index_without_version_bump` (1 test passed)
  - `scripts\clean_builds.ps1` reported `Before: 1.5 GB`, `After: 1.5 GB`.
- [x] Latest focused migration/schema equivalence rerun:
  - Confirmed the performance-index ensure path is idempotent, runs only after the schema-too-new guard, and matches fresh-schema hot-path indexes.
  - `cargo test -p smriti --lib db::migrations::tests::run_migrations_adds_timeline_order_index_without_version_bump` (1 test passed)
  - `cargo test -p smriti --lib db::schema::tests::test_create_schema` (1 test passed)
- [x] Latest focused face-job registration rerun:
  - Tightened People and Settings face-processing starts so route unmounts do not drop the real job chip after the backend returns.
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
- [x] Latest focused job-starter placeholder rerun:
  - Tightened Timeline, Albums, Duplicates, Bursts, and Settings job starters so placeholders are always dismissed after IPC returns and same-library route navigation keeps real job chips.
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
- [x] Latest focused album-export path rerun:
  - Album export now re-resolves each DB relative path through canonical library-root containment immediately before copy.
  - `cargo fmt --all`
  - `npm ci --prefix src-ui`
  - `npm run build --prefix src-ui`
  - `cargo test -p smriti-tauri --lib albums` (5 tests passed)
  - `scripts\clean_builds.ps1` reported `Before: 1.5 GB`, `After: 1.5 GB`.
- [x] Latest focused startup-bundle rerun:
  - Lazy-loaded `PhotoDetail.svelte` and `Map.svelte` from `App.svelte`.
  - Production build split MapLibre into a separate lazy chunk and reduced initial JS from about 2.16 MB to about 1.01 MB.
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `npm run build --prefix src-ui`
- [x] Latest broad gate rerun:
  - `npm run check` from `src-ui`
  - `npm run build` from `src-ui`
  - `npm run test` from `src-ui` (10 files, 81 tests)
  - `cargo test -p smriti -p smriti-tauri`
  - `cargo clippy --all-targets -p smriti -p smriti-tauri -- -D warnings`
- [x] Latest current broad gate rerun after library-scope/schema-preflight fixes:
  - `cargo fmt --all --check`
  - `git diff --check` (CRLF warnings only)
  - `npm ci --prefix src-ui`
  - `npm run check --prefix src-ui` (0 errors, 0 warnings)
  - `npm run test --prefix src-ui` (10 files, 86 tests)
  - `npm audit --audit-level=high --prefix src-ui` (0 vulnerabilities)
  - `npm run build --prefix src-ui`
  - `cargo test -p smriti -p smriti-tauri` (passed)
  - `cargo clippy --all-targets -p smriti -p smriti-tauri -- -D warnings`
  - `cargo audit` (0 vulnerabilities; 19 allowed warnings)
- [x] Latest final consistency/slop scan:
  - Suspicious-marker scan found no `test.only`, `describe.only`, `debugger`, `console.log`, TODO/FIXME/HACK/XXX leftovers in active source/docs; matches were legitimate placeholder text/SQL placeholder terms only.
  - Full frontend command parse found 126 invoked Tauri commands, 153 registered handlers, and 0 missing handlers.
  - Wrapper-only API parse found 123 wrapper commands and 0 missing handlers.
  - Script line-ending normalization remains content-neutral under `git diff --ignore-space-at-eol`.
  - Workflow scan confirms GitHub Actions use Node `20.19`; stale Node 20+/Vite 5 docs are gone. The remaining `20.18.1` hit is a transitive package-lock engine, not project guidance.
- [x] Latest command-surface documentation parity:
  - Updated `docs/COMMAND_SURFACE.md` for active library/photos/people/albums/map/geocoding/system commands and removed stale rows for removed commands.
  - Parser found 153 documented commands, 153 registered Tauri handlers, 141 frontend-invoked commands, and 0 missing in any direction.
  - Stale docs scan found no `documents.run_analysis`, `geocoding.run`, `library.regenerate_rotated_data`, `people.rebuild_clusters`, `people.set_representative_face`, `system.updates.download`, or `system.updates.install` rows.
  - `git diff --check -- docs/COMMAND_SURFACE.md AUDIT_TODO.md`
- [x] Release metadata/dependency sanity:
  - `npm audit --audit-level=high` found 0 vulnerabilities.
  - Package/config metadata checked; apparent mojibake was terminal rendering, not corrupt file text.
  - Removed stale tracked `src-tauri/package-lock.json`; there is no matching `src-tauri/package.json`, and the real frontend lockfile lives in `src-ui/`.
  - Tauri capability file checked: active frontend plugin use is covered by `core:default` and `dialog:default`.
  - `cargo check -p smriti-tauri` passed after removing the stale shell lockfile.
  - Toolchain snapshot: Rust/Cargo 1.95.0, Node 24.15.0, npm 11.12.1, Tauri CLI 2.11.1.
  - Updated stale `AGENTS.md` dependency note from Vite 5 to Vite 8.
  - Added `src-ui` Node engine guard: `^20.19.0 || ^22.12.0 || >=24.0.0`, matching Vite/Rolldown requirements.
  - Pinned GitHub Actions Node setup to `20.19` instead of generic `20`.
  - Updated build docs from Node 20+ to Node 20.19+.
  - `npm install --package-lock-only --ignore-scripts`, `npm ci`, `npm run check`, `npm run build`, `npm run test` (10 files, 81 tests), and `npm audit --audit-level=high` passed after the engine guard.
  - Rerun docs/workflow scan found no stale Node 20+ or Vite 5 references in active docs/workflows.
- [x] Push-gate script hygiene:
  - Fixed Windows CRLF breakage risk for bash scripts by adding `.gitattributes` rules for `*.sh` and `scripts/pre-push`.
  - Normalized tracked shell scripts to LF; `git diff --ignore-space-at-eol` shows no content changes for those scripts.
  - `bash -n scripts/ci_local.sh scripts/pre-push` passed.
  - `scripts/ci_local.sh --help` runs under login bash.
  - A full login-bash `scripts/ci_local.sh ci` attempt exceeded the tool timeout on the WSL/Windows bridge; the same check set is covered by the latest native manual gate above.
- [x] Build cleanup run after Cargo threshold
  - Latest Windows cleanup script reported `Before: 0`.
  - Removed ignored frontend artifacts created by verification (`src-ui/dist`, `src-ui/node_modules`) after checks completed.
  - Latest Windows cleanup script reported `Before: 1.5 GB`, `After: 1.5 GB`.
  - Latest Windows cleanup script after newer-schema preflight test reported `Before: 1.5 GB`, `After: 1.5 GB`.
  - Latest Windows cleanup script after current broad gate reported `Before: 1.5 GB`, `After: 1.5 GB`.

## Pending Final Gates

- [x] Re-run command/API parity check.
- [x] Re-run runtime panic/TODO/unwrap scan.
- [x] Run broader local CI gate when the audit settles.
- [x] Re-run `cargo audit` if dependencies changed after the previous fix.
- [x] Do a final diff review for accidental broad/sloppy changes.
  - `git diff --check` passes.
  - No focused frontend tests (`test.only`, `describe.only`, etc.) found.
  - Suspicious-token scan found only legitimate AI/settings strings, thumbnail constants, and tracker text.
  - Diff remains broad because the audit covered many surfaces; review evidence does not make it a small patch.
  - Latest hunk-by-hunk review of all changed source files found no broad decorative changes or weak edits to remove.
- [x] Decide and document true backward-compatible read-only library-open strategy.
  - Safe unsupported-library shell implemented; settings/update path is the conference fallback.

## Known Remaining Work

- [x] Implement schema-too-new compatibility shell.
  - Uses a separate non-writable library state.
  - Do not route unsupported schemas through normal `OpenLibrary`.
- [x] Allow-listed read-only photo preview for newer schemas.
  - Uses `library_compat_photos_list`.
  - Reads only stable `photos` columns from a read-only SQLite connection.
  - Shows existing thumbnails only; it does not generate thumbnails or mutate the library.

## Future Expansion

- [~] Full read-only normal-shell browsing for newer schemas.
  - Not a conference blocker after the compatibility shell and preview.
  - Requires schema-by-schema compatibility proof for each command before exposing it.

## File Inventory Audit

Status here is stricter than the module audit above:
- [x] File-level audited in this ledger; any confirmed issue fixed or documented.
- Dedicated source-file inventory pass is complete for the current tree.

Current inventory: 183 source files (`src`, `src-tauri/src`, `src-ui/src`).

- [x] `src\bin\build_geonames.rs`
- [x] `src\bootstrap.rs`
- [x] `src\config\mod.rs`
- [x] `src\db\album_repo.rs`
- [x] `src\db\album_suggestion_repo.rs`
- [x] `src\db\burst_repo.rs`
- [x] `src\db\connection.rs`
- [x] `src\db\document_repo.rs`
- [x] `src\db\duplicate_repo.rs`
- [x] `src\db\excluded_folder_repo.rs`
- [x] `src\db\face_repo\gallery.rs`
- [x] `src\db\face_repo\mod.rs`
- [x] `src\db\face_repo\read.rs`
- [x] `src\db\face_repo\write.rs`
- [x] `src\db\geonames.rs`
- [x] `src\db\inferred_identity_repo.rs`
- [x] `src\db\migrations.rs`
- [x] `src\db\mod.rs`
- [x] `src\db\photo_repo.rs`
- [x] `src\db\recent_search_repo.rs`
- [x] `src\db\schema.rs`
- [x] `src\db\stack_repo.rs`
- [x] `src\db\trash_repo.rs`
- [x] `src\error.rs`
- [x] `src\lib.rs`
- [x] `src\ml\alignment.rs`
- [x] `src\ml\clustering.rs`
- [x] `src\ml\face_detector.rs`
- [x] `src\ml\face_embedder.rs`
- [x] `src\ml\mod.rs`
- [x] `src\ml\remote_embedder.rs`
- [x] `src\ml\resolver.rs`
- [x] `src\ml\retrieval.rs`
- [x] `src\ml\runtime.rs`
- [x] `src\models\mod.rs`
- [x] `src\models\photo.rs`
- [x] `src\models\timeline_group.rs`
- [x] `src\search\date_parser.rs`
- [x] `src\search\mod.rs`
- [x] `src\search\query_parser.rs`
- [x] `src\services\album_suggestions.rs`
- [x] `src\services\assistant.rs`
- [x] `src\services\burst_detector.rs`
- [x] `src\services\camera_names.rs`
- [x] `src\services\document_detector.rs`
- [x] `src\services\drive_detector.rs`
- [x] `src\services\duplicate_detector.rs`
- [x] `src\services\exclusions.rs`
- [x] `src\services\exif_extractor.rs`
- [x] `src\services\face_processor.rs`
- [x] `src\services\face_processor\clustering.rs`
- [x] `src\services\geocoding.rs`
- [x] `src\services\image_io.rs`
- [x] `src\services\image_utils.rs`
- [x] `src\services\insights.rs`
- [x] `src\services\install_method.rs`
- [x] `src\services\library_health.rs`
- [x] `src\services\map_math.rs`
- [x] `src\services\memories.rs`
- [x] `src\services\metadata_processor.rs`
- [x] `src\services\mod.rs`
- [x] `src\services\ocr_processor.rs`
- [x] `src\services\path_util.rs`
- [x] `src\services\photo_stacks.rs`
- [x] `src\services\raw_preview.rs`
- [x] `src\services\reindexer.rs`
- [x] `src\services\scanner.rs`
- [x] `src\services\search.rs`
- [x] `src\services\self_replace.rs`
- [x] `src\services\semantic.rs`
- [x] `src\services\thumbnail.rs`
- [x] `src\services\tile_cache.rs`
- [x] `src\services\trash.rs`
- [x] `src\services\update_checker.rs`
- [x] `src\utils.rs`
- [x] `src-tauri/src\commands\albums.rs`
- [x] `src-tauri/src\commands\assistant.rs`
- [x] `src-tauri/src\commands\bursts.rs`
- [x] `src-tauri/src\commands\documents.rs`
- [x] `src-tauri/src\commands\duplicates.rs`
- [x] `src-tauri/src\commands\geocoding.rs`
- [x] `src-tauri/src\commands\health.rs`
- [x] `src-tauri/src\commands\insights.rs`
- [x] `src-tauri/src\commands\library.rs`
- [x] `src-tauri/src\commands\map.rs`
- [x] `src-tauri/src\commands\memories.rs`
- [x] `src-tauri/src\commands\mod.rs`
- [x] `src-tauri/src\commands\people.rs`
- [x] `src-tauri/src\commands\photos.rs`
- [x] `src-tauri/src\commands\search.rs`
- [x] `src-tauri/src\commands\semantic.rs`
- [x] `src-tauri/src\commands\settings.rs`
- [x] `src-tauri/src\commands\stacks.rs`
- [x] `src-tauri/src\commands\system.rs`
- [x] `src-tauri/src\commands\trash.rs`
- [x] `src-tauri/src\dto.rs`
- [x] `src-tauri/src\error.rs`
- [x] `src-tauri/src\events.rs`
- [x] `src-tauri/src\jobs.rs`
- [x] `src-tauri/src\lib.rs`
- [x] `src-tauri/src\main.rs`
- [x] `src-tauri/src\pagination.rs`
- [x] `src-tauri/src\state.rs`
- [x] `src-tauri/src\thumbnail_upgrade.rs`
- [x] `src-ui/src\app.css`
- [x] `src-ui/src\App.svelte`
- [x] `src-ui/src\assets\smriti-logo.svg`
- [x] `src-ui/src\assets\smriti-logomark.svg`
- [x] `src-ui/src\lib\actions\marqueeSelect.ts`
- [x] `src-ui/src\lib\api\all.ts`
- [x] `src-ui/src\lib\api\events.ts`
- [x] `src-ui/src\lib\api\index.test.ts`
- [x] `src-ui/src\lib\api\index.ts`
- [x] `src-ui/src\lib\api\library.ts`
- [x] `src-ui/src\lib\api\photos.ts`
- [x] `src-ui/src\lib\api\system.ts`
- [x] `src-ui/src\lib\api\types.ts`
  - Aligned `PersonDto` with the Rust wire DTO by replacing the stale `face_count` field with `representative_face_id`.
- [x] `src-ui/src\lib\components\AddToAlbumDialog.svelte`
- [x] `src-ui/src\lib\components\AssistantDrawer.svelte`
- [x] `src-ui/src\lib\components\DetailHeader.svelte`
- [x] `src-ui/src\lib\components\FaceCell.svelte`
- [x] `src-ui/src\lib\components\JobsIndicator.svelte`
- [x] `src-ui/src\lib\components\KSimilarDialog.svelte`
- [x] `src-ui/src\lib\components\MergePersonDialog.svelte`
- [x] `src-ui/src\lib\components\PageHeader.svelte`
- [x] `src-ui/src\lib\components\ReassignFaceDialog.svelte`
- [x] `src-ui/src\lib\components\SelectionBar.svelte`
- [x] `src-ui/src\lib\components\Sidebar.svelte`
- [x] `src-ui/src\lib\components\Slideshow.svelte`
- [x] `src-ui/src\lib\components\ToastHost.svelte`
- [x] `src-ui/src\lib\components\ZoomImage.svelte`
- [x] `src-ui/src\lib\decodeOffscreen.test.ts`
- [x] `src-ui/src\lib\decodeOffscreen.ts`
- [x] `src-ui/src\lib\dominantColor.ts`
- [x] `src-ui/src\lib\slideshowQueue.test.ts`
- [x] `src-ui/src\lib\slideshowQueue.ts`
- [x] `src-ui/src\lib\stores\assistant.svelte.ts`
- [x] `src-ui/src\lib\stores\browseContext.svelte.ts`
- [x] `src-ui/src\lib\stores\browseContext.test.ts`
- [x] `src-ui/src\lib\stores\devMode.svelte.ts`
- [x] `src-ui/src\lib\stores\devMode.test.ts`
- [x] `src-ui/src\lib\stores\jobs.svelte.ts`
- [x] `src-ui/src\lib\stores\jobs.test.ts`
- [x] `src-ui/src\lib\stores\library.svelte.ts`
- [x] `src-ui/src\lib\stores\photoVisibility.svelte.ts`
- [x] `src-ui/src\lib\stores\selection.svelte.ts`
- [x] `src-ui/src\lib\stores\selection.test.ts`
- [x] `src-ui/src\lib\stores\settings.svelte.ts`
- [x] `src-ui/src\lib\stores\slideshow.svelte.ts`
- [x] `src-ui/src\lib\stores\slideshow.test.ts`
- [x] `src-ui/src\lib\stores\toast.svelte.ts`
- [x] `src-ui/src\lib\thumbnail.test.ts`
- [x] `src-ui/src\lib\thumbnail.ts`
- [x] `src-ui/src\lib\thumbnailQueue.test.ts`
- [x] `src-ui/src\lib\thumbnailQueue.ts`
- [x] `src-ui/src\lib\thumbnailRequest.ts`
- [x] `src-ui/src\lib\tile-cache.ts`
- [x] `src-ui/src\lib\videoProbe.ts`
- [x] `src-ui/src\lib\virtualizer.svelte.ts`
- [x] `src-ui/src\lib\zoomApi.ts`
- [x] `src-ui/src\main.ts`
- [x] `src-ui/src\routes\AlbumDetail.svelte`
- [x] `src-ui/src\routes\Albums.svelte`
- [x] `src-ui/src\routes\BurstDetail.svelte`
- [x] `src-ui/src\routes\Bursts.svelte`
- [x] `src-ui/src\routes\DuplicateDetail.svelte`
- [x] `src-ui/src\routes\Duplicates.svelte`
- [x] `src-ui/src\routes\FaceReview.svelte`
- [x] `src-ui/src\routes\Insights.svelte`
- [x] `src-ui/src\routes\Map.svelte`
- [x] `src-ui/src\routes\Memories.svelte`
- [x] `src-ui/src\routes\MemoryDetail.svelte`
- [x] `src-ui/src\routes\People.svelte`
- [x] `src-ui/src\routes\PersonDetail.svelte`
- [x] `src-ui/src\routes\PersonReview.svelte`
- [x] `src-ui/src\routes\PhotoDetail.svelte`
- [x] `src-ui/src\routes\Search.svelte`
- [x] `src-ui/src\routes\Settings.svelte`
- [x] `src-ui/src\routes\Shortcuts.svelte`
- [x] `src-ui/src\routes\Timeline.svelte`
- [x] `src-ui/src\routes\Trash.svelte`
- [x] `src-ui/src\routes\Welcome.svelte`
- [x] `src-ui/src\vite-env.d.ts`
