# State Machine

UI state is modeled in `src/app/state/mod.rs`.

Messages in `src/app/messages.rs` drive updates through handlers in
`src/app/handlers/`, and rendering happens via `src/app/views.rs`.
