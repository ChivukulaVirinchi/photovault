# Timeline

Timeline is the main grid view grouped by day.

Keyboard support:
- Arrow keys move highlight
- `Enter` opens highlighted photo
- `Space` toggles selection
- `PageUp` / `PageDown` / `Home` / `End` scroll the timeline
